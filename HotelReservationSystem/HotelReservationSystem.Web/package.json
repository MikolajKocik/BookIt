{
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "sass": "sass --watch wwwroot/scss:wwwroot/scss --no-source-map",
    "ts": "tsc --watch",
    "watch": "concurrently \"npm run sass\" \"npm run ts\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "sass": "^1.80.0",
    "chokidar": "^3.6.0"
  }
}
